# Copy this file to secrets-local.properties and fill in real values.
# This file is safe to commit because it contains only placeholders.

# Minimum recommendation: 64 random bytes encoded as Base64.
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_BASE64_SECRET

# Optional local/demo admin bootstrap (only if explicitly needed):
# app.bootstrap.admin.enabled=true
# app.bootstrap.admin.email=admin@example.local
# app.bootstrap.admin.password=REPLACE_WITH_STRONG_ADMIN_PASSWORD
